import { type Result } from "../../reporter";

const results: Result[] = [
	{
		filePath: "empty.html",
		errorCount: 0,
		warningCount: 0,
		source: null,
		messages: [],
	},
];

export default results;
