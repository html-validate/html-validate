import { type ConfigData } from "../config-data";

/** @public */
const config: ConfigData = {
	rules: {
		"attr-quotes": "off",
		"doctype-style": "off",
		"void-style": "off",
	},
};

export default config;
